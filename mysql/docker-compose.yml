version: "2"
services:
  mysql:
    image: mysql:5.7
    ports:
      - "3306:3306"
    environment:
      - "MYSQL_ROOT_PASSWORD=3299216138"
      #      - "MYSQL_DATABASE=root"
      - "MYSQL_ROOT_HOST=%"
      - "TZ=Asia/Shanghai"
    hostname: mysql
    container_name: mysql
    privileged: true